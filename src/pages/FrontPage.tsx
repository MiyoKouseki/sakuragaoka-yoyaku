import React from 'react';
import { Typography, Container } from '@mui/material';

const WelcomeComponent: React.FC = () => {
  return (
    <Container>
      <Typography variant="h1">ようこそ</Typography>
    </Container>
  );
};

export default WelcomeComponent;
